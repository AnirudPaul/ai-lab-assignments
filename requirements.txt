# Versions this record was produced with (Python 3.11, Windows 11, RTX 4060).
numpy==2.1.3
pandas==2.2.3
matplotlib==3.9.2
seaborn==0.13.2
scikit-learn==1.6.1
scipy==1.14.1
networkx==3.4.2
pillow==12.1.1
pyarrow==16.1.0

# PyTorch with CUDA 12.1. For CPU-only, install plain `torch`/`torchvision` instead:
#   pip install torch torchvision
# For this CUDA build:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
torchvision==0.20.1

# Notebook assembly, execution and export
nbformat==5.11.1
nbclient
nbconvert==7.17.1
ipykernel==6.31.0
playwright            # only needed for PDF export via `--to webpdf`
